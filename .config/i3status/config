# i3status configuration file.
# see "man i3status" for documentation.

# It is important that this file is edited as UTF-8.
# The following line should contain a sharp s:
# ß
# If the above line is not correctly displayed, fix your editor first!

general {
	colors = true
        interval = 5
	color_good = "#9ece6a"     # Tokyo Night Green
        color_degraded = "#e0af68" # Tokyo Night Yellow
        color_bad = "#f7768e"      # Tokyo Night Red
}

# Define which modules to load and in what order
order += "disk /"
order += "cpu_usage"
order += "memory"
order += "ethernet _first_"
order += "tztime local"

# Module Configurations
disk "/" {
        format = " HDD: %avail "
}

cpu_usage {
        format = " CPU: %usage "
}

memory {
        format = " RAM: %used "
        threshold_degraded = "10%"
}

ethernet _first_ {
        # Only shows IP if connected. Drops the unnecessary speed metrics.
        format_up = " ETH: %ip "
        format_down = " ETH: down "
}

tztime local {
       # Formats time to 12-hour: YYYY-MM-DD HH:MM AM/PM
        format = " %Y-%m-%d %I:%M %p "
}
